# ==========================================
# KONFIGURASI DATABASE (PostgreSQL lokal / Docker)
# Di docker-compose, hostname service = "db"
# ==========================================
DATABASE_URL="postgresql://postgres:postgres@db:5432/point_sekolah"
DIRECT_URL="postgresql://postgres:postgres@db:5432/point_sekolah"

# ==========================================
# KONFIGURASI NEXTAUTH
# NEXTAUTH_SECRET: generate dengan: openssl rand -base64 32
# NEXTAUTH_URL: URL publik aplikasi (HTTPS di production)
# ==========================================
NEXTAUTH_SECRET="ganti_dengan_random_string_32_karakter"
NEXTAUTH_URL="https://point-sekolah.rofimain.com"

# Postgres container (dipakai docker-compose.yml)
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="point_sekolah"

# ==========================================
# KONFIGURASI DOMAIN SEKOLAH
# Sesuaikan dengan domain email sekolah Anda
# ==========================================
NEXT_PUBLIC_SCHOOL_NAME="SMA ISLAM AL AZHAR 1 JAKARTA"
NEXT_PUBLIC_SCHOOL_SHORT="SK"
NEXT_PUBLIC_STUDENT_DOMAIN="siswa.sman1contoh.sch.id"
NEXT_PUBLIC_STAFF_DOMAIN="sman1contoh.sch.id"

# ==========================================
# KONFIGURASI POIN KRITIS SISWA
# ==========================================
NEXT_PUBLIC_CRITICAL_POINTS=75
NEXT_PUBLIC_WARNING_POINTS=50
# Pelanggaran dengan poin STRICT di atas nilai ini wajib foto dan/atau tanda tangan/pengakuan murid
NEXT_PUBLIC_HEAVY_VIOLATION_POINTS=20

# Password awal untuk siswa baru (tambah satuan / impor bulk), bila tidak diisi di form
# DEFAULT_STUDENT_PASSWORD="Siswa@1234"

# Cron: remisi otomatis 25% setelah 30 hari tenang sejak tanggal KEJADIAN pelanggaran.
# Override opsional hari tenang (default 30) lewat env saja — tidak ada setting UI.
# Dipanggil service Compose `cron` harian.
# CRON_SECRET="isi_random_panjang"
# POINT_REDUCTION_QUIET_DAYS=30
# TZ=Asia/Jakarta
# NEXT_PUBLIC_INCIDENT_TIMEZONE="Asia/Jakarta"

# Bot Telegram: notifikasi ke orang tua saat ada catatan pelanggaran (siswa atau guru). Token dari @BotFather
# TELEGRAM_BOT_TOKEN="123456:ABC-DEF..."
# Username bot (tanpa @) — untuk tautan ortu t.me/bot?start=ortu_...  + respons API setelah tambah siswa
# NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="sekolah_pelanggaran_bot"
# Rahasia webhook (opsional, disarankan): hanya A-Z a-z 0-9 _ - (1-256 char). Bukan token bot. Contoh: openssl rand -hex 32
# TELEGRAM_WEBHOOK_SECRET="a1b2c3d4e5f6789012345678901234567890abcdef"
